spring:
  application:
    name: orm
    datasource:
      url: jdbc:postgresql://localhost:5432/orm_demo
      username: root
      password: postgres
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true
